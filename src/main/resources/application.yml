server:
  port: 9090

spring:
  profiles:
    group:
      local: db, jwt, mail
    active: local
  application:
    name: secure-login

  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect